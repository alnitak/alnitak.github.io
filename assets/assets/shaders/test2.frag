{
  "sksl": {
    "entrypoint": "test2_fragment_main",
    "shader": "// This SkSL shader is autogenerated by spirv-cross.\n\nfloat4 flutter_FragCoord;\n\nuniform vec2 uResolution;\nuniform float iTime;\nuniform shader iChannel0;\nuniform half2 iChannel0_size;\n\nvec4 fragColor;\n\nvec3 iResolution;\n\nvec2 FLT_flutter_local_FlutterFragCoord()\n{\n    return flutter_FragCoord.xy;\n}\n\nvec3 FLT_flutter_local_B2_spline(vec3 x)\n{\n    vec3 t = x * 3.0;\n    vec3 b0 = step(vec3(0.0), t) * step(vec3(0.0), vec3(1.0) - t);\n    vec3 b1 = step(vec3(0.0), t - vec3(1.0)) * step(vec3(0.0), vec3(2.0) - t);\n    vec3 b2 = step(vec3(0.0), t - vec3(2.0)) * step(vec3(0.0), vec3(3.0) - t);\n    return (((b0 * pow(t, vec3(2.0))) + (b1 * (((pow(t, vec3(2.0)) * (-2.0)) + (t * 6.0)) - vec3(3.0)))) + (b2 * pow(vec3(3.0) - t, vec3(2.0)))) * 0.5;\n}\n\nvoid FLT_flutter_local_mainImage(out vec4 fragColor_1, vec2 fragCoord)\n{\n    vec2 uv = fragCoord / iResolution.xy;\n    uv = vec2(uv.x, 1.0 - uv.y);\n    float fVBars = 100.0;\n    float fHSpacing = 1.0;\n    float fHFreq = uv.x * 3.1400001049041748046875;\n    float squarewave = sign((sin(fHFreq * fVBars) + 1.0) - fHSpacing);\n    float x = floor(uv.x * fVBars) / fVBars;\n    float fSample = iChannel0.eval(iChannel0_size * ( vec2(abs((2.0 * x) - 1.0), 0.25))).x;\n    float fft = (squarewave * fSample) * 0.5;\n    float fHBars = 100.0;\n    float fVSpacing = 0.180000007152557373046875;\n    float fVFreq = uv.y * 3.1400001049041748046875;\n    fVFreq = sign((sin(fVFreq * fHBars) + 1.0) - fVSpacing);\n    vec2 centered = (vec2(1.0) * uv) - vec2(1.0);\n    float t = iTime / 100.0;\n    float polychrome = 1.0;\n    vec3 spline_args = fract(vec3((polychrome * uv.x) - t) + vec3(0.0, -0.3333333432674407958984375, -0.666666686534881591796875));\n    vec3 param = spline_args;\n    vec3 spline = FLT_flutter_local_B2_spline(param);\n    float f = abs(centered.y);\n    vec3 base_color = vec3(1.0) - (spline * f);\n    vec3 flame_color = pow(base_color, vec3(3.0));\n    float tt = 0.300000011920928955078125 - uv.y;\n    float df = sign(tt);\n    df = (df + 1.0) / 0.5;\n    vec3 col = (flame_color * vec3(1.0 - step(fft, abs(0.300000011920928955078125 - uv.y)))) * vec3(fVFreq);\n    col -= ((col * df) * 0.180000007152557373046875);\n    fragColor_1 = vec4(col, 1.0);\n}\n\nvoid FLT_main()\n{\n    iResolution = vec3(uResolution.x, uResolution.y, 0.0);\n    vec2 param_2 = FLT_flutter_local_FlutterFragCoord();\n    vec4 param_1;\n    FLT_flutter_local_mainImage(param_1, param_2);\n    fragColor = param_1;\n}\n\nhalf4 main(float2 iFragCoord)\n{\n      flutter_FragCoord = float4(iFragCoord, 0, 0);\n      FLT_main();\n      return fragColor;\n}\n",
    "stage": 1,
    "uniforms": [
      {
        "array_elements": 0,
        "bit_width": 0,
        "columns": 1,
        "location": 0,
        "name": "iChannel0",
        "rows": 1,
        "type": 12
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 1,
        "name": "uResolution",
        "rows": 2,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 2,
        "name": "iTime",
        "rows": 1,
        "type": 10
      }
    ]
  }
}